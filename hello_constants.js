```javascript
const PI = Math.PI;

// 1. The Geometry of Light (Fine Structure Constant)
//    Projection of a 3-Sphere: Volume + Surface + Line
const alpha_inv = 4*Math.pow(PI, 3) + Math.pow(PI, 2) + PI;

// 2. The Geometry of Mass (Proton/Electron Ratio)
//    Projection of a 5-Sphere: Volume
const mp_me = 6 * Math.pow(PI, 5);

console.log("Hello, Universe.");
console.log("----------------");
console.log(`Light (1/α): ${alpha_inv.toFixed(4)}  (Reality: 137.0360)`);
console.log(`Mass (mp/me): ${mp_me.toFixed(4)} (Reality: 1836.1527)`);
```
